const User = require('./User');
const Recruiter = require('./Recruiter'); 
const Developer = require('./Developer'); 
const Job = require('./Jobs')
const Donation = require('./Donation')

module.exports = { User, Recruiter, Developer, Job, Donation };
